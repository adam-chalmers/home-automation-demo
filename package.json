{
  "name": "@adam-chalmers/personal-server",
  "version": "1.0.0",
  "description": "A personal home server.",
  "author": "Adam Chalmers <adam.chalmers93@gmail.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adam-chalmers/wol.git"
  },
  "main": "dist/index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "ts-node-dev ./src/index.ts",
    "start:dev": "node -r ts-node/register ./src/index.ts",
    "start": "node ./dist/index.js",
    "prepare": "tsc --build --clean && tsc",
    "deploy": "yarn publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/wol": "^1.0.0",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@adam-chalmers/google-home": "^1.0.0",
    "@adam-chalmers/network-monitor": "^1.0.1",
    "@adam-chalmers/wol": "^1.0.0",
    "chokidar": "^3.4.3",
    "dotenv": "^8.2.0"
  }
}
